[phases.setup]
nixPkgs = ["...", "dejavu_fonts", "fontconfig", "cairo", "pango", "libjpeg", "giflib", "pixman", "pkg-config", "python3"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "fc-cache -f 2>/dev/null; node dist/index.js"